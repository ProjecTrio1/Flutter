import 'package:flutter/material.dart';

class AIRecipeRecommendationPage extends StatelessWidget {
  const AIRecipeRecommendationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI 레시피 추천')),
      body: const Center(
        child: Text('asdf'),
      ),
    );
  }
}